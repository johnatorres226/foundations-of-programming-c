/* Harness self-test fixture. Not part of the course. An unfinished stub. */
#include <stdio.h>
#include <stdlib.h>

int subtract(int a, int b) {
    (void) a;
    (void) b;
    fprintf(stderr, "TODO: implement subtract() in exercises/02_wrong.c\n");
    exit(1);
}
