/* Harness self-test fixture. Not part of the course. An unfinished stub. */
#include <stdio.h>
#include <stdlib.h>

int race_count(int num_threads, int increments_per_thread) {
    (void) num_threads;
    (void) increments_per_thread;
    fprintf(stderr, "TODO: implement race_count() in exercises/03_race.c\n");
    exit(1);
}
