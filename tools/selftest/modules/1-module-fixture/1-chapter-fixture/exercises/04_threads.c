/* Harness self-test fixture. Not part of the course. An unfinished stub. */
#include <stdio.h>
#include <stdlib.h>

int run(void *arg) {
    (void) arg;
    fprintf(stderr, "TODO: implement run() in exercises/04_threads.c\n");
    exit(1);
}
